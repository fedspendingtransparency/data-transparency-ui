import {Meta, Canvas, Controls} from '@storybook/blocks';
import * as CardStories from "./cardButton.stories";
import CardButton from "../../components/cards/CardButton";

<Meta of={CardStories} />


# Card
```javascript
import { CardButton } from 'data-transparency-ui';
```

export const Template = (args) => <CardButton {...args} />;

## Controls
<Controls of={CardStories.Default} />
