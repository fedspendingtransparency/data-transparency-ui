import {Meta, Story, Canvas, ArgsTable} from '@storybook/addon-docs';
import CardContainer from '../../components/cards/CardContainer';
import CardBody from "../../components/cards/CardBody";
import CardButton from "../../components/cards/CardButton";
import CardHero from "../../components/cards/CardHero";

<Meta title="Card/Default" component={CardContainer}
 parameters={{options: {showAddonPanel: false}}} />

# Card
```javascript
import { CardContainer, CardHero, CardBody, CardButton } from 'data-transparency-ui';
```

Cards are UI Elements that contain content and actions about a single subject

export const Template = (args) => <CardContainer {...args} />;

## Containers Variants
<Canvas>
    <Story id="card--fill" />
    <Story id="card--elevated" />
    <Story id="card--outline" />
    <Story inline={false} id="card--with-card-button" />
</Canvas>

## Card Container
<ArgsTable story={CardContainer} />

## Hero Variants
<Canvas>
  <Story inline={false} id="card--hero-fill-with-image" />
  <Story inline={false} id="card--hero-with-image" />
  <Story inline={false} id="card--hero-with-bar-fill" />
</Canvas>

## Card Hero
<ArgsTable story={CardHero} />

## Card Body 
<ArgsTable story={CardBody} />

## Card Button 
<ArgsTable story={CardButton} />
