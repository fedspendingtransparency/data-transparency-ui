import React from 'react';
import { Button } from '../../components/Button';

export default { title: 'Button' };

export const defaultButton = () => (
  <div className="button-story__container">
    <Button text="Click Yo Self Always" />
  </div>
);
