import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { boolean, object, } from "@storybook/addon-knobs";
import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';
import LoadingMessage from '../../components/messages/LoadingMessage';
import ErrorMessage from '../../components/messages/ErrorMessage';
import NoResultsMessage from '../../components/messages/NoResultsMessage';
import ComingSoon from '../../components/messages/ComingSoon';
import GenericMessage from '../../components/messages/GenericMessage';

<Meta
    title="Messages"
    component={GenericMessage}
/>

## Generic Message Props:
 - `className`: A way to customize message stlyling. Gets appended to `usda-message_` to create a _modifier_ class in BEM methodology.
 - `title`: The first (bolded) line of text.
 - `description`: The second line of text.
 - `icon`: An icon to display above the text.

## Generic Message
<Canvas>
    <Story name='Generic Message'>
        <GenericMessage
            className="your-class"
            title="Your title"
            description="Custom description here."
            icon={<FontAwesomeIcon icon="pencil-alt" />} />
    </Story>
</Canvas>

<ArgsTable of={GenericMessage} />

## Loading Message
<Canvas>
    <Story name='Loading Message'>
        <LoadingMessage />
    </Story>
</Canvas>

## Default Error Message
<Canvas>
    <Story name='Error Message'>
        <ErrorMessage />
    </Story>
</Canvas>

<ArgsTable of={ErrorMessage} />

## Custom Error Message
<Canvas>
    <Story name='Custom Error Message'>
        <ErrorMessage title="Your title" description="Custom error message here." />
    </Story>
</Canvas>

## No Results Message
<Canvas>
    <Story name='No Results Message'>
        <NoResultsMessage />
    </Story>
</Canvas>

## Coming Soon Placeholder
<Canvas>
    <Story name="Coming Soon Placeholder">
        <ComingSoon />
    </Story>
</Canvas>

## Integration Status
Answers the question, where is this component used?

### USASpending
- [x] Agency Submission Statistics
- [x] Agency Profile v2

